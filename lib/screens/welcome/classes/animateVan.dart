import 'package:flutter/material.dart';

class AnimateVan extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
    return Container(
      child: Image.asset("assets/images/camioneta.png"),
     );
   }
}